# MotoPartes WhatsApp Bot — environment variables template.
# Copy to `.env` (never commit the real one) or set these in Dokploy.

# Same Postgres as the API — the bot reads/writes the whatsapp_sessions table.
DATABASE_URL="postgresql://motopartes:change-me@localhost:5432/motopartes"

# HTTP port Express listens on. Dockerfile exposes 3002.
PORT=3002

# Shared secret checked in the x-api-key header on every request except /health
# (and /debug only if WHATSAPP_BOT_DEBUG_PUBLIC=true). Must match the API's
# WHATSAPP_API_KEY. The legacy name API_KEY is still accepted for backwards compatibility.
WHATSAPP_API_KEY=
# API_KEY=   # legacy alias — prefer WHATSAPP_API_KEY
WHATSAPP_BOT_DEBUG_PUBLIC=false

# Puppeteer / Chromium. The Dockerfile installs system Chromium and wires these.
# In local dev on a machine that already has Chrome, you can leave them unset
# and Puppeteer will download its own browser.
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
CHROME_PATH=/usr/bin/chromium

# Path where whatsapp-web.js persists each mechanic's session data. In the
# container this is mounted as a Docker volume so sessions survive restarts.
WWEBJS_DATA_PATH=/app/data/wwebjs_auth

# WhatsApp Web version handling.
# auto = pick the newest archived WPPConnect build that actually has HTML.
# latest/none = bypass the archive and load live https://web.whatsapp.com.
# exact version = pin manually, e.g. 2.3000.1040081378-alpha.
WWEBJS_WEB_VERSION=auto
WWEBJS_VERSION_MANIFEST_URL=https://raw.githubusercontent.com/wppconnect-team/wa-version/main/versions.json
WWEBJS_VERSION_HTML_BASE_URL=https://raw.githubusercontent.com/wppconnect-team/wa-version/main/html

# Stability/recovery tuning.
WWEBJS_AUTH_TIMEOUT_MS=180000
WWEBJS_INIT_TIMEOUT_MS=210000
WWEBJS_QR_MAX_RETRIES=6
WWEBJS_RESTART_BASE_DELAY_MS=15000
WWEBJS_RESTART_MAX_DELAY_MS=600000
