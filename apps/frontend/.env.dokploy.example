# .env.dokploy (Ejemplo para conectar local -> remoto)

# URL de la API desplegada en Dokploy (https)
VITE_API_URL=https://api.tu-dominio.com/api

# El bot NO se debe acceder directo si es posible, idealmente usas la API como proxy.
# Si tu frontend necesita hablar directo (no recomendado), usa la URL del bot.
# Pero con la arquitectura actual, todo pasa por la API.
# VITE_WHATSAPP_BOT_URL=https://bot.tu-dominio.com

# Auth
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
